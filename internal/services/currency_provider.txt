package services

import (
	"context"

	"github.com/shopspring/decimal"
)

type MoexClient interface {
	GetCurrnetPrice(ctx context.Context, isin string) (amount decimal.Decimal, err error)
}

type CurrencyProvider struct {
	moexClient MoexClient
}

func NewCurrencyProvider(
	moexClient MoexClient,
) CurrencyProvider {
	return CurrencyProvider{
		moexClient: moexClient,
	}
}

rules:
	areas:
		1: etalon_a%
		2: etalon_b%
		3: etalon_c%

common:
	areas:
		1: a% 10
		2: b% 30
		3: c% 60
	portfolios:
		1:
			avaliable_areas: 1, 2
			money: 100 (10%, 10%)
		2:
			avaliable_areas: 2, 3
			money: 200 (20%, 60%)


к какой суммме на портфелях мы идем - это плева

math:


300 = a% + x , 300/a * 100 + x 

sum/a*etalon_a - sum = x - x/100*etalon_a

sum * (etalon_a / a - 1) = x (1 - etalon_a / 100)
sum * (etalon_a - a) / a = x * (100 - etalon_a) / 100
x = sum * (etalon_a - a) * 100 / (a * (100 - etalon_a))


sum + all_new_sum:
	etalon_a_amount:
	etalon_b_amount:
	etalon_c_amount:


отдельно лежат стоимости бумаг на конкретный момент времени:
	тикер - стоимость (комиссия???)

значит у нас будет эталонная структура, которая образуется из новой суммы и эталонных процентов:
	тикер - сумма - количество  что ещё??? можно ли только сумму?
	кажется надо оперировать количествами, потому что проще понимать, мы купили всё, что можно или нет

	то есть храним количество, сколько надо купить, и идем резолвим, каждый по отдельности тикер?

и структура, которая будет обозначать портфель: (или писать всё в эталонный)
	портфели:
		портфель:
			тикер1: количество
			


algo:
	идем по эталонным областям и пытаемся их докупить:
	1. если область надо докупать (там есть тикеры, надо правильно по ним распределить):
		1.1 распределение по тикерам нет, делим в равно соотношении
		1.2 распределение по тикерам есть, делим по распределению
	2. если область докупать не надо, то двигаемся к следующей области:
		2.1 такое может быть, если в этой области произошел прирост, а в других убыток
	3. если остались непотраченные деньги:
		такое например может произойти, если область одну докупать не надо, а другую добили до нужного значения меньшей суммой?
		или мы не смогли докупить до нужной суммы область, потому что правила (суммы) пополнения портфеля этой области не хватает
		3.1 собираем все тикеры, которые можем купить с их коефициентами
			3.1.1 можем купить = суммы, которые остались по каждому портфелю... (надо как-то аккуратно учитывать, с какого портфеля, что платим, в предыдущих пунктах???)
		3.2 покупаем относительно коэффициентов
		3.3 это должен быть 1 проход, после которого на оставшиеся деньги в каждом портфеле мы просто покупаем LQDT? или вводим соответствующие правило, прсто кэш храним, например
		А У МЕНЯ НЕТ ПОЗИЦИИ КЭША ВРОДЕ БЫ)))
	4. какая-то проверка, что мы все покупаем правильно:
		- приблизительное количество бумаг
		- приблезительная стоимость бумаг
		- приблезительные проценты (какой-то delute? +- 0.05%)
	какие-то бумаги могут стоить 3к, а какие-то 3 рубля, должна быть нормализация же??
	то есть должен быть какой-то приоритет покупок???
	если покупка бумаги на 3к превысит эталонный процент, то надо купить или нет?

ответ:
	map[portfolioID]struct{
		mainBuies: []struct{
			isin: string,
			count: int,
			amount: decimal.Decimal,
		},
		lostMoney: decimal.Decimal,
	}

